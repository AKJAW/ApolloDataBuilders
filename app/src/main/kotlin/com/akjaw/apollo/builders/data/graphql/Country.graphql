query Country($code: ID!) {
    countrySchema: country(code: $code) {
        name
        languagesSchemas: languages {
            ...LanguageFragment
        }
    }
}

fragment LanguageFragment on Language {
    name
}
